import { css } from '../styled-system/css';

// Refactor ProfileCard in 'ProfileCard.tsx' to use patterns

// Fetch profile data from 'profile-data.ts'

// Build page and render ProfileCard  within App

function App() {
  return (
    <div
      className={css({
        minH: '100dvh',
      })}
    ></div>
  );
}

export default App;
